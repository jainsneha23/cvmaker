import {schema} from 'normalizr';

// export const storeLocation = new schema.Entity('storeLocation');
// export const store = new schema.Entity('store', {location: storeLocation});
// export const stores = {stores: [{location: storeLocation}]};

// export const facilityTimetable = new schema.Entity('facilityTimetables', {}, {idAttribute: 'name'});
// export const facilityTimetables = [facilityTimetable];
// export const storeDetails = new schema.Entity('storeDetails', {facilityTimetables}, {idAttribute: 'storeId'});
